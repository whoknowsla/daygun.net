server {
    listen 80;
    listen [::]:80;
    server_name daygun.net www.daygun.net;

    # Redirect HTTP to HTTPS (uncomment after SSL setup)
    # return 301 https://$server_name$request_uri;

    # For initial testing without SSL
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    location /static/ {
        alias /home/sun/Documents/Projects/daygun.net/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /media/ {
        alias /home/sun/Documents/Projects/daygun.net/media/;
        expires 30d;
    }

    client_max_body_size 10M;
}

# HTTPS configuration (after getting SSL certificate with certbot)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name daygun.net www.daygun.net;
#
#     ssl_certificate /etc/letsencrypt/live/daygun.net/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/daygun.net/privkey.pem;
#     
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /static/ {
#         alias /home/sun/Documents/Projects/daygun.net/staticfiles/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /media/ {
#         alias /home/sun/Documents/Projects/daygun.net/media/;
#         expires 30d;
#     }
#
#     client_max_body_size 10M;
# }
